iterator: AnimalPose.train_keypoint_predictor.Iterator
model: AnimalPose.models.ResPoseNet
batch_size: 16
num_steps: 60001
lr: 0.0001
cuda: true  # False -> use GPU
ckpt_freq: 2000
#Training methods
pretrained: True
resnet_type: 18 #50,101,152
adjust_learning_rate: False
# Pretraining!
load_self_pretrained_encoder:
  active: True
  path: "logs/2020-05-12T13-05-49_LosscontrainedLAMBDAINIT1mioREAL/train/checkpoints/model-74000.ckpt"
  #path: "load_weights/chkpt_020-04-27T16-50-52_DIS_FREEZEEED/model-80000.ckpt"
load_self_pretrained_decoder:
  active: False
  path: "logs/2020-05-12T13-05-49_LosscontrainedLAMBDAINIT1mioREAL/train/checkpoints/model-74000.ckpt"
# Encoder is frozen by default
freeze_encoder_for: 99999999999999 #steps helps to train the decoder in first place


#### BELOW THIS ALL CONFIGS ARE FORE KEYPOINT PREDICTORS THE SAME!
# Heatmap
hm:
  # we consider a keypoint to be a keypoint only above thresh
  thresh: 0.01 # 0.01 is a good choice, removes noise in the heatmaps
# PCK
pck:
  pck_multi: True
  type: "object" # "object" or "image"

# Losses
losses:
  L1: False
  L2: True
  KL: False
  perceptual: True
  perceptual_network: "vgg" # alex, squeeze or vgg
  perceptual_weight: 1.0
  #VGGL1Loss
  vgg: False
  vgg_l1_alpha: 1.0
  vgg_alpha: 1.0
# logging
integrations:
  git:
    active: False
  wandb:
    active: True
  tensorboard:
    active: True
